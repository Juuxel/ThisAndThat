/* This file is a part of the This & That project
 * by Juuxel, licensed under the MIT license.
 * Full code and license: https://github.com/Juuxel/ThisAndThat
 */
package juuxel.thisandthat.util

/*import net.minecraft.block.enums.BlockHalf
import net.minecraft.util.math.Direction
import net.shadowfacts.simplemultipart.util.MultipartPlacementContext*/

object MultipartUtils {
    /*fun getHalf(context: MultipartPlacementContext): BlockHalf {
        val hitSide = context.facing
        val hitY = context.hitY

        return when {
            hitSide === Direction.DOWN -> if (hitY >= 0.5f) BlockHalf.BOTTOM else BlockHalf.TOP
            hitSide === Direction.UP -> if (0.5f <= hitY && hitY < 1) BlockHalf.TOP else BlockHalf.BOTTOM
            else -> if (hitY >= 0.5f) BlockHalf.TOP else BlockHalf.BOTTOM
        }
    }*/
}